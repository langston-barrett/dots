conky.config = {
    alignment="top_left",
    background=false,
    use_xft=true,
    font="Oxygen-Sans:size=18";
    override_utf8_locale=true;
    update_interval=1,
    total_run_times=0,
    -- this doesn't work with argb
    -- double_buffer=true,

    -- WINDOW
    own_window=true,
    own_window_type="override",
    own_window_transparent=true,
    own_window_argb_visual=true,
    own_window_argb_value=0;
    own_window_hints="undecorated,skip_taskbar,skip_pager,sticky,below",
    minimum_width=3200,
    maximum_width=3200,
    default_color="#F5F6F7",
    default_outline_color="#999",

    -- TEXT
    draw_borders=false,
    draw_outline=false,
    draw_shades=false,

    -- cpu_avg_samples=2,
    -- net_avg_samples=2,

    -- lua_load="~/.config/conky/rings-v1.2.2.lua",
    -- lua_draw_hook_pre="conky_ring_stats",
}

-- # ${font Noto Emoji:normal:size=20}
-- # CPU 💪
-- # Memory 🐏
-- # Battery ⚡
-- # Disk 💿
-- # Volume 🔈

conky.text = [[
    ${font Oxygen-Sans:normal:size=80}${tztime America/Los_Angeles %H:%M}
    ${font Oxygen-Sans:normal:size=16}
    ${voffset -200}${tztime America/Los_Angeles %Y // %m // %d}
    mem: ${memperc}
    bat: ${battery_percent BAT1} ${if_match "${acpiacadapter}" == "on-line"}(+)${endif}
    fs: ${fs_used_perc /}
    cpu: ${cpu cpu0}
]]
