#!/usr/bin/env bash

set -e

if [[ ${OSTYPE} == darwin* ]]; then
  # This requires giving notification permission to "Script Editor"
  txt=$(printf 'display notification "" with title "%s"' "${1}")
  osascript -e "${txt}"
else
  notify-send --expire-time=2500 "${1}" 2>&1 > /dev/null
fi
