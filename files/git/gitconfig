######## General

[user]
    name = Langston Barrett
    email = langston.barrett@gmail.com
    signingkey = 5608B8E7A68F0445
[core]
    excludesfile = /home/langston/.config/git/gitignore
	autocrlf = false
[grep]
    extendedRegexp = true
[color]
    ui = auto
[push]
    default = simple

######## Pre-commit hooks

# TODO: whitespace too
[init]
    templatedir = /home/langston/.config/git
	defaultBranch = main

######## Credentials and URLs

[credential "https://github.com"]
    username = langston-barrett
[credential "http://github.com"]
    username = langston-barrett
[credential "https://gitlab.com"]
    username = langston-barrett
[credential "http://gitlab.com"]
    username = langston-barrett
[url "ssh://git@github.com/"]
    insteadOf = https://github.com/

#### Galois Gitlab

[url "ssh://git@gitlab-int.galois.com/"]
    insteadOf = https://gitlab-int.galois.com/
[url "ssh://git@gitlab-ext.galois.com/"]
    insteadOf = https://gitlab-ext.galois.com/

######## Aliases

[alias]
	br = branch
	co = checkout
	cob = checkout -b
	comn = checkout main
	coms = checkout master
	dc = diff --cached
	fa = fetch --all
	l = log
	head = rev-parse HEAD
	head- = rev-parse HEAD~
	ph = push
	phf = push --force-with-lease
	pl = pull
	plom = pull origin main
	plomr = pull origin master
	plou = pull upstream main
	plour = pull upstream master
	pushf = push --force-with-lease
	rem = remote -v
	remotev = remote -v
	rv = remote -v
    prup = "!f() { git fetch upstream pull/${1}/head:pr-${1}; git checkout pr-${1}; }; f"
    re = rebase
    rei = rebase -i
    reim = rebase -i master
    s = status
    undo = reset HEAD~1 --mixed

######## Conditional includes

# [includeIf "gitdir:~/code/v2v"]
#     path = ~/.config/git/galois
# [gitlab "gitlab-ext.galois.com/api/v4"]
# 	user = langston
[http]
	sslVerify = false
[pull]
	ff = only
